--- origsrc/qt-x11-opensource-src-4.4.0/src/corelib/global/qnamespace.h	2008-04-28 08:11:18.000000000 -0500
+++ src/qt-x11-opensource-src-4.4.0/src/corelib/global/qnamespace.h	2008-04-28 08:11:00.000000000 -0500
@@ -1041,7 +1041,7 @@
     };
 #endif // Q_OS_MAC
 
-#if defined(Q_OS_WIN) || defined(Q_OS_CYGWIN)
+#if defined(Q_OS_WIN)
 #ifndef qdoc
     typedef int WindowsVersion;
 
diff -urN -x CYGWIN-PATCHES -x 'aclocal.m4~' -x aclocal.m4t -x autom4te.cache -x config.cache -x config.log -x config.status -x config.h -x '*config.h.in' -x ABOUT-NLS -x Makefile.in.in -x Makevars.template -x '*SlackBuild*' -x '*.egg-info' -x '*.class' -x '*.pyc' -x '*.mo' -x '*.gmo' -x '*.orig' -x '*.rej' -x '*.spec' -x '*.temp' -x '*~' -x '*.stackdump' -x COPYING -x INSTALL -x compile -x config-ml.in -x config.guess -x config.sub -x depcomp -x elisp-comp -x install-sh -x libtool.m4 -x ltoptions.m4 -x ltsugar.m4 -x ltversion.m4 -x 'lt~obsolete.m4' -x ltmain.sh -x mdate-sh -x missing -x mkinstalldirs -x py-compile -x symlink-tree -x texinfo.tex -x ylwrap -x config.rpath -x omf.make -x xmldocs.make -x gnome-doc-utils.make -x gnome-doc-utils.m4 -x intltool.m4 -x intltool-extract -x intltool-extract.in -x intltool-merge -x intltool-merge.in -x intltool-update -x intltool-update.in -x demos -x doc -x examples -x clucene -x freetype -x harfbuzz -x kdebase -x kdelibs -x libjpeg -x libmng -x libpng -x libtiff -x sqlite -x wintab -x zlib origsrc/qt-x11-opensource-src-4.4.0/src/corelib/global/qglobal.cpp src/qt-x11-opensource-src-4.4.0/src/corelib/global/qglobal.cpp
--- origsrc/qt-x11-opensource-src-4.4.0/src/corelib/global/qglobal.cpp	2008-04-28 08:11:18.000000000 -0500
+++ src/qt-x11-opensource-src-4.4.0/src/corelib/global/qglobal.cpp	2008-07-13 14:04:56.125000000 -0500
@@ -1620,7 +1620,7 @@
 }
 const QSysInfo::MacVersion QSysInfo::MacintoshVersion = macVersion();
 
-#elif defined(Q_OS_WIN32) || defined(Q_OS_CYGWIN) || defined(Q_OS_WINCE)
+#elif defined(Q_OS_WIN32) || defined(Q_OS_WINCE)
 
 QT_BEGIN_INCLUDE_NAMESPACE
 #include "qt_windows.h"
diff -urN -x CYGWIN-PATCHES -x 'aclocal.m4~' -x aclocal.m4t -x autom4te.cache -x config.cache -x config.log -x config.status -x config.h -x '*config.h.in' -x ABOUT-NLS -x Makefile.in.in -x Makevars.template -x '*SlackBuild*' -x '*.egg-info' -x '*.class' -x '*.pyc' -x '*.mo' -x '*.gmo' -x '*.orig' -x '*.rej' -x '*.spec' -x '*.temp' -x '*~' -x '*.stackdump' -x COPYING -x INSTALL -x compile -x config-ml.in -x config.guess -x config.sub -x depcomp -x elisp-comp -x install-sh -x libtool.m4 -x ltoptions.m4 -x ltsugar.m4 -x ltversion.m4 -x 'lt~obsolete.m4' -x ltmain.sh -x mdate-sh -x missing -x mkinstalldirs -x py-compile -x symlink-tree -x texinfo.tex -x ylwrap -x config.rpath -x omf.make -x xmldocs.make -x gnome-doc-utils.make -x gnome-doc-utils.m4 -x intltool.m4 -x intltool-extract -x intltool-extract.in -x intltool-merge -x intltool-merge.in -x intltool-update -x intltool-update.in -x demos -x doc -x examples -x clucene -x freetype -x harfbuzz -x kdebase -x kdelibs -x libjpeg -x libmng -x libpng -x libtiff -x sqlite -x wintab -x zlib origsrc/qt-x11-opensource-src-4.4.0/src/corelib/io/qfsfileengine_iterator_unix.cpp src/qt-x11-opensource-src-4.4.0/src/corelib/io/qfsfileengine_iterator_unix.cpp
--- origsrc/qt-x11-opensource-src-4.6.2/src/corelib/io/qfsfileengine_iterator_unix.cpp
+++ src/qt-x11-opensource-src-4.6.2/src/corelib/io/qfsfileengine_iterator_unix.cpp
@@ -55,7 +55,7 @@
 public:
     inline QFSFileEngineIteratorPlatformSpecificData()
         : dir(0), dirEntry(0), done(false)
-#if defined(_POSIX_THREAD_SAFE_FUNCTIONS) && !defined(Q_OS_CYGWIN) && !defined(Q_OS_SYMBIAN)
+#if defined(_POSIX_THREAD_SAFE_FUNCTIONS) && !defined(Q_OS_SYMBIAN)
           , mt_file(0)
 #endif
     { }
@@ -64,7 +64,7 @@
     dirent *dirEntry;
     bool done;
 
-#if defined(_POSIX_THREAD_SAFE_FUNCTIONS) && !defined(Q_OS_CYGWIN) && !defined(Q_OS_SYMBIAN)
+#if defined(_POSIX_THREAD_SAFE_FUNCTIONS) && !defined(Q_OS_SYMBIAN)
     // for readdir_r
     dirent *mt_file;
 #endif
@@ -77,7 +77,7 @@
     if (!platform->dir)
         return;
 
-#if defined(_POSIX_THREAD_SAFE_FUNCTIONS) && !defined(Q_OS_CYGWIN) && !defined(Q_OS_SYMBIAN)
+#if defined(_POSIX_THREAD_SAFE_FUNCTIONS) && !defined(Q_OS_SYMBIAN)
     if (::readdir_r(platform->dir, platform->mt_file, &platform->dirEntry) != 0)
         platform->done = true;
 #else
@@ -88,7 +88,7 @@
         ::closedir(platform->dir);
         platform->dir = 0;
         platform->done = true;
-#if defined(_POSIX_THREAD_SAFE_FUNCTIONS) && !defined(Q_OS_CYGWIN) && !defined(Q_OS_SYMBIAN)
+#if defined(_POSIX_THREAD_SAFE_FUNCTIONS) && !defined(Q_OS_SYMBIAN)
         delete [] platform->mt_file;
         platform->mt_file = 0;
 #endif
@@ -104,7 +104,7 @@
 {
     if (platform->dir) {
         ::closedir(platform->dir);
-#if defined(_POSIX_THREAD_SAFE_FUNCTIONS) && !defined(Q_OS_CYGWIN) && !defined(Q_OS_SYMBIAN)
+#if defined(_POSIX_THREAD_SAFE_FUNCTIONS) && !defined(Q_OS_SYMBIAN)
         delete [] platform->mt_file;
         platform->mt_file = 0;
 #endif
@@ -125,7 +125,7 @@
             if ((int) maxPathName == -1)
                 maxPathName = FILENAME_MAX;
             maxPathName += sizeof(dirent) + 1;
-#if defined(_POSIX_THREAD_SAFE_FUNCTIONS) && !defined(Q_OS_CYGWIN) && !defined(Q_OS_SYMBIAN)
+#if defined(_POSIX_THREAD_SAFE_FUNCTIONS) && !defined(Q_OS_SYMBIAN)
             if (that->platform->mt_file)
                 delete [] that->platform->mt_file;
             that->platform->mt_file = (dirent *)new char[maxPathName];
--- origsrc/qt-x11-opensource-src-4.4.0/src/gui/kernel/qapplication.h	2008-04-28 08:11:18.000000000 -0500
+++ src/qt-x11-opensource-src-4.4.0/src/gui/kernel/qapplication.h	2008-07-13 14:14:59.343750000 -0500
@@ -303,7 +303,7 @@
     enum { NormalColors = NormalColor, CustomColors = CustomColor };
     static inline QT3_SUPPORT ColorMode colorMode() { return static_cast<ColorMode>(colorSpec()); }
     static inline QT3_SUPPORT void setColorMode(ColorMode mode) { setColorSpec(int(mode)); }
-#if defined(Q_OS_WIN32) || defined(Q_OS_CYGWIN)
+#if defined(Q_OS_WIN32)
     static QT3_SUPPORT Qt::WindowsVersion winVersion() { return (Qt::WindowsVersion)QSysInfo::WindowsVersion; }
 #endif
 #if defined(Q_OS_MAC)
--- origsrc/qt-x11-opensource-src-4.4.0/src/gui/kernel/qapplication_p.h	2008-04-28 08:11:18.000000000 -0500
+++ src/qt-x11-opensource-src-4.4.0/src/gui/kernel/qapplication_p.h	2008-07-13 14:07:28.656250000 -0500
@@ -86,7 +86,7 @@
 extern QClipboard *qt_clipboard;
 #endif
 
-#if defined (Q_OS_WIN32) || defined (Q_OS_CYGWIN) || defined(Q_OS_WINCE)
+#if defined (Q_OS_WIN32) || defined(Q_OS_WINCE)
 extern QSysInfo::WinVersion qt_winver;
 enum { QT_TABLET_NPACKETQSIZE = 128 };
 # ifdef Q_OS_WINCE
diff -urN -x CYGWIN-PATCHES -x 'aclocal.m4~' -x aclocal.m4t -x autom4te.cache -x config.cache -x config.log -x config.status -x config.h -x '*config.h.in' -x ABOUT-NLS -x Makefile.in.in -x Makevars.template -x '*SlackBuild*' -x '*.egg-info' -x '*.class' -x '*.pyc' -x '*.mo' -x '*.gmo' -x '*.orig' -x '*.rej' -x '*.spec' -x '*.temp' -x '*~' -x '*.stackdump' -x COPYING -x INSTALL -x compile -x config-ml.in -x config.guess -x config.sub -x depcomp -x elisp-comp -x install-sh -x libtool.m4 -x ltoptions.m4 -x ltsugar.m4 -x ltversion.m4 -x 'lt~obsolete.m4' -x ltmain.sh -x mdate-sh -x missing -x mkinstalldirs -x py-compile -x symlink-tree -x texinfo.tex -x ylwrap -x config.rpath -x omf.make -x xmldocs.make -x gnome-doc-utils.make -x gnome-doc-utils.m4 -x intltool.m4 -x intltool-extract -x intltool-extract.in -x intltool-merge -x intltool-merge.in -x intltool-update -x intltool-update.in -x demos -x doc -x examples -x clucene -x freetype -x harfbuzz -x kdebase -x kdelibs -x libjpeg -x libmng -x libpng -x libtiff -x sqlite -x wintab -x zlib origsrc/qt-x11-opensource-src-4.4.0/src/qt3support/network/q3dns.cpp src/qt-x11-opensource-src-4.4.0/src/qt3support/network/q3dns.cpp
--- origsrc/qt-x11-opensource-src-4.4.0/src/qt3support/network/q3dns.cpp	2008-04-28 08:11:19.000000000 -0500
+++ src/qt-x11-opensource-src-4.4.0/src/qt3support/network/q3dns.cpp	2008-07-13 16:12:10.750000000 -0500
@@ -43,7 +43,7 @@
 
 #include "qplatformdefs.h"
 #include "qbytearray.h"
-#if defined(Q_OS_WIN32) || defined(Q_OS_WINCE) || defined(Q_OS_CYGWIN)
+#if defined(Q_OS_WIN32) || defined(Q_OS_WINCE)
 # include "qt_windows.h"
 #else
 # include <sys/types.h>
@@ -2206,7 +2206,7 @@
 }
 #endif
 
-#if defined(Q_OS_WIN32) || defined(Q_OS_CYGWIN)
+#if defined(Q_OS_WIN32)
 
 #if defined(Q_DNS_SYNCHRONOUS)
 void Q3Dns::doSynchronousLookup()
